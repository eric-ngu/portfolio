ERIC NGU PORTFOLIO — V70 FULL CLOUDFLARE DEPLOY

Commands:
  npm install
  npm run dev
  npm run deploy

This project preserves the same npm/Wrangler deployment workflow as the prior full-deploy package.
Builder Sans is prepared automatically from an existing v69/v68 deploy folder placed beside this folder, or from public/assets/fonts if already present.
